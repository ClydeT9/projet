<style>
#connexion {
    display:none;
}
body {                /* body - or any parent wrapper */
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
.container {
  flex: 1;
}
.body2a {
    background: white;
    padding-top: 20px;
    padding-bottom: 20px;
    width: 50%;
    margin: auto;
    margin-top: 50px;
}
</style>
  <div class="body2a">
                  <div class="card-title">Réinitialisation du mot de passe</div>
                 <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                    <div class="form-group">
                    <label class="form-control-placeholder">Adresse email</label>
                        <%= f.email_field :email, autocomplete: "email",class:"form-control", :required => true %>
                    </div>
                      <div class="actions">
                        <%= f.submit "Réinitialisation du mot de passe",class:"btn btn-primary btn-block" %>
                      </div>
                  <% end %>
                </div><%#  body2a %>